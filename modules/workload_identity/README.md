# Workload Identity

Creates a GitHub Actions Workload Identity Federation pool, provider, Terraform runner service accounts, and impersonation bindings scoped by repository and branch.

Secret JSON keys should not be created for these service accounts.

