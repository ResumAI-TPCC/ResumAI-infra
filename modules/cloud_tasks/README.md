# Cloud Tasks

Creates or imports one Cloud Tasks queue with explicit dispatch, concurrency,
retry, and logging settings. The module does not enable the Cloud Tasks API;
API activation and queue creation require an intentional environment change.
