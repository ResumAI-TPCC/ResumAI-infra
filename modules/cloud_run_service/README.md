# Cloud Run Service

Manages a Cloud Run service shell for import. Revision template changes are ignored during the import phase to avoid storing existing plaintext environment secrets in Terraform state/config.

