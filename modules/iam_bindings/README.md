# IAM Bindings

Manages additive project and service-account IAM members. It intentionally
uses `*_iam_member` resources so adopting one binding cannot replace unrelated
members maintained by Google Cloud or another team.
